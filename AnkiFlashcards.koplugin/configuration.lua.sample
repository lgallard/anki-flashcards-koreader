local CONFIGURATION = {
    -- DashScope (Qwen) — used for both card text generation and image generation.
    -- Get your key at: https://dashscope-intl.console.aliyun.com/
    dashscope_api_key = "YOUR_DASHSCOPE_API_KEY",
    provider          = "https://dashscope-intl.aliyuncs.com/compatible-mode/v1/chat/completions",
    model             = "qwen-plus",

    -- ── Anki integration ────────────────────────────────────────────────────
    -- Requires AnkiConnect add-on running on your Mac.
    -- Use your Mac's LAN IP — not "localhost".
    -- Note type MUST be "English" (the anki-card-generator note type).
    anki = {
        url   = "http://192.168.0.62:8765",  -- ← set your Mac's LAN IP
        deck  = "English::Koreader",
        model = "English",
        tags  = { "KOReader" },
    },
}

return CONFIGURATION
